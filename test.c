#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <string.h>

int main(int argc, char const *argv[])
{
  printf("\nArquivo rodou!\n");

  // char* args = strdup("a b c");

  // char *cmd = "ls";
  // char *argv2[3];
  // argv2[0] = "";
  // argv2[1] = "";
  // argv2[2] = NULL;

  // execvp(cmd, argv2); 
  // for(int i = 0;i < argc; i++) {
  //   printf("%s", argv[i]);
  // }
  return 0;
}
